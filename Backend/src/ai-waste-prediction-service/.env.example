# ------------------------------------------------------------------
# ai-waste-prediction-service — environment variables
# Copy this file to `.env` and adjust the values for your environment.
# ------------------------------------------------------------------

# Port the Express server listens on (frontend expects 8000).
PORT=8001

# MongoDB connection string.
# Local default:
MONGO_URI=ghjhgk
# Atlas example:
# MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/dry-fish-backend
